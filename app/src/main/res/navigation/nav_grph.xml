<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_grph"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.lifeorganizer.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" >
        <action
            android:id="@+id/budgetTransact"
            app:destination="@id/budgetActivity"
            app:enterAnim="@anim/fragment_close_enter" />
    </activity>
    <activity
        android:id="@+id/budgetActivity"
        android:name="com.example.lifeorganizer.BudgetActivity"
        android:label="BudgetActivity" />
</navigation>